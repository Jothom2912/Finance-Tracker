apiVersion: apps/v1
kind: Deployment
metadata:
  name: banking-service
  namespace: finance-tracker
spec:
  replicas: 1
  selector:
    matchLabels: { app: banking-service }
  template:
    metadata:
      labels: { app: banking-service }
    spec:
      containers:
        - name: banking-service
          image: finance-tracker/banking-service:local
          imagePullPolicy: Never
          envFrom:
            - configMapRef:
                name: finance-tracker-config
            - secretRef:
                name: finance-tracker-secrets
          env:
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: finance-tracker-secrets
                  key: DATABASE_URL_BANKING
          ports:
            - containerPort: 8009
          readinessProbe:
            httpGet:
              path: /health
              port: 8009
            initialDelaySeconds: 20
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 8009
            initialDelaySeconds: 40
            periodSeconds: 20
---
apiVersion: v1
kind: Service
metadata:
  name: banking-service
  namespace: finance-tracker
spec:
  selector: { app: banking-service }
  ports:
    - port: 8009
      targetPort: 8009