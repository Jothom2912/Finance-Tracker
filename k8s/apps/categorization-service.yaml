apiVersion: apps/v1
kind: Deployment
metadata:
  name: categorization-service
  namespace: finance-tracker
spec:
  replicas: 1
  selector:
    matchLabels: { app: categorization-service }
  template:
    metadata:
      labels: { app: categorization-service }
    spec:
      containers:
        - name: categorization-service
          image: finance-tracker/categorization-service:local
          imagePullPolicy: Never
          envFrom:
            - configMapRef:
                name: finance-tracker-config
            - secretRef:
                name: finance-tracker-secrets
          env:
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: finance-tracker-secrets
                  key: DATABASE_URL_CATEGORIZATION
          ports:
            - containerPort: 8005
          readinessProbe:
            httpGet:
              path: /health
              port: 8005
            initialDelaySeconds: 20
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 8005
            initialDelaySeconds: 40
            periodSeconds: 20
---
apiVersion: v1
kind: Service
metadata:
  name: categorization-service
  namespace: finance-tracker
spec:
  selector: { app: categorization-service }
  ports:
    - port: 8005
      targetPort: 8005
