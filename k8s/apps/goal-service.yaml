apiVersion: apps/v1
kind: Deployment
metadata:
  name: goal-service
  namespace: finance-tracker
spec:
  replicas: 1
  selector:
    matchLabels: { app: goal-service }
  template:
    metadata:
      labels: { app: goal-service }
    spec:
      containers:
        - name: goal-service
          image: finance-tracker/goal-service:local
          imagePullPolicy: Never
          envFrom:
            - configMapRef:
                name: finance-tracker-config
            - secretRef:
                name: finance-tracker-secrets
          env:
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: finance-tracker-secrets
                  key: DATABASE_URL_GOAL
          ports:
            - containerPort: 8006
          readinessProbe:
            httpGet:
              path: /health
              port: 8006
            initialDelaySeconds: 20
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 8006
            initialDelaySeconds: 40
            periodSeconds: 20
---
apiVersion: v1
kind: Service
metadata:
  name: goal-service
  namespace: finance-tracker
spec:
  selector: { app: goal-service }
  ports:
    - port: 8006
      targetPort: 8006
