apiVersion: batch/v1
kind: Job
metadata:
  name: serverless-health-init
  namespace: finance-tracker
spec:
  backoffLimit: 3
  template:
    spec:
      restartPolicy: Never
      containers:
        - name: serverless-health-init
          image: finance-tracker/serverless-health-job:local
          imagePullPolicy: Never
          command: ["python", "-m", "app.main", "init"]
          envFrom:
            - secretRef:
                name: finance-tracker-secrets
          env:
            - name: QUEUE_NAME
              value: serverless.health.requests
