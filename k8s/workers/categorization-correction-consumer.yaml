apiVersion: apps/v1
kind: Deployment
metadata:
  name: categorization-correction-consumer
  namespace: finance-tracker
spec:
  replicas: 1
  selector:
    matchLabels: { app: categorization-correction-consumer }
  template:
    metadata:
      labels: { app: categorization-correction-consumer }
    spec:
      containers:
        - name: categorization-correction-consumer
          image: finance-tracker/categorization-service:local
          imagePullPolicy: Never
          command: ["python", "-m", "app.workers.category_corrected_consumer"]
          envFrom:
            - configMapRef:
                name: finance-tracker-config
            - secretRef:
                name: finance-tracker-secrets
          env:
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: finance-tracker-secrets
                  key: DATABASE_URL_CATEGORIZATION
