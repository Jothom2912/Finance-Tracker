apiVersion: apps/v1
kind: Deployment
metadata:
  name: account-sync-consumer
  namespace: finance-tracker
spec:
  replicas: 1
  selector:
    matchLabels: { app: account-sync-consumer }
  template:
    metadata:
      labels: { app: account-sync-consumer }
    spec:
      containers:
        - name: account-sync-consumer
          image: finance-tracker/monolith:local
          imagePullPolicy: Never
          command: ["python", "-m", "backend.consumers.worker", "--consumer", "account-sync"]
          envFrom:
            - configMapRef:
                name: finance-tracker-config
            - secretRef:
                name: finance-tracker-secrets
          env:
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: finance-tracker-secrets
                  key: DATABASE_URL_MONOLITH
