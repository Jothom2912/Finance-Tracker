apiVersion: apps/v1
kind: Deployment
metadata:
  name: categorization-category-sync
  namespace: finance-tracker
spec:
  replicas: 1
  selector:
    matchLabels: { app: categorization-category-sync }
  template:
    metadata:
      labels: { app: categorization-category-sync }
    spec:
      containers:
        - name: categorization-category-sync
          image: finance-tracker/categorization-service:local
          imagePullPolicy: Never
          command: ["python", "-m", "app.workers.category_sync_consumer"]
          envFrom:
            - configMapRef:
                name: finance-tracker-config
            - secretRef:
                name: finance-tracker-secrets
          env:
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: finance-tracker-secrets
                  key: DATABASE_URL_CATEGORIZATION
