apiVersion: apps/v1
kind: Deployment
metadata:
  name: transaction-taxonomy-consumer
  namespace: finance-tracker
spec:
  replicas: 1
  selector:
    matchLabels: { app: transaction-taxonomy-consumer }
  template:
    metadata:
      labels: { app: transaction-taxonomy-consumer }
    spec:
      containers:
        - name: transaction-taxonomy-consumer
          image: finance-tracker/transaction-service:local
          imagePullPolicy: Never
          command: ["python", "-m", "app.workers.taxonomy_sync_consumer"]
          envFrom:
            - configMapRef:
                name: finance-tracker-config
            - secretRef:
                name: finance-tracker-secrets
          env:
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: finance-tracker-secrets
                  key: DATABASE_URL_TRANSACTION
