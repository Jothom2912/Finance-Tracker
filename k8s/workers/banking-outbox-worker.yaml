apiVersion: apps/v1
kind: Deployment
metadata:
  name: banking-outbox-worker
  namespace: finance-tracker
spec:
  replicas: 1
  selector:
    matchLabels: { app: banking-outbox-worker }
  template:
    metadata:
      labels: { app: banking-outbox-worker }
    spec:
      containers:
        - name: banking-outbox-worker
          image: finance-tracker/banking-service:local
          imagePullPolicy: Never
          command: ["python", "-m", "app.workers.outbox_publisher"]
          envFrom:
            - configMapRef:
                name: finance-tracker-config
            - secretRef:
                name: finance-tracker-secrets
          env:
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: finance-tracker-secrets
                  key: DATABASE_URL_BANKING