apiVersion: apps/v1
kind: Deployment
metadata:
  name: transaction-sync-consumer
  namespace: finance-tracker
spec:
  replicas: 1
  selector:
    matchLabels: { app: transaction-sync-consumer }
  template:
    metadata:
      labels: { app: transaction-sync-consumer }
    spec:
      containers:
        - name: transaction-sync-consumer
          image: finance-tracker/monolith:local
          imagePullPolicy: Never
          command: ["python", "-m", "backend.consumers.worker", "--consumer", "transaction-sync"]
          envFrom:
            - configMapRef:
                name: finance-tracker-config
            - secretRef:
                name: finance-tracker-secrets
          env:
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: finance-tracker-secrets
                  key: DATABASE_URL_MONOLITH
