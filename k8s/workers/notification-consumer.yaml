apiVersion: apps/v1
kind: Deployment
metadata:
  name: notification-consumer
  namespace: finance-tracker
spec:
  replicas: 1
  selector:
    matchLabels: { app: notification-consumer }
  template:
    metadata:
      labels: { app: notification-consumer }
    spec:
      containers:
        - name: notification-consumer
          image: finance-tracker/notification-service:local
          imagePullPolicy: Never
          command: ["python", "-m", "app.workers.notification_consumer"]
          envFrom:
            - configMapRef:
                name: finance-tracker-config
            - secretRef:
                name: finance-tracker-secrets
          env:
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: finance-tracker-secrets
                  key: DATABASE_URL_NOTIFICATION
