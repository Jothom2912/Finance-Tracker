apiVersion: apps/v1
kind: Deployment
metadata:
  name: goal-outbox-worker
  namespace: finance-tracker
spec:
  replicas: 1
  selector:
    matchLabels: { app: goal-outbox-worker }
  template:
    metadata:
      labels: { app: goal-outbox-worker }
    spec:
      containers:
        - name: goal-outbox-worker
          image: finance-tracker/goal-service:local
          imagePullPolicy: Never
          command: ["python", "-m", "app.workers.outbox_publisher"]
          envFrom:
            - configMapRef:
                name: finance-tracker-config
            - secretRef:
                name: finance-tracker-secrets
          env:
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: finance-tracker-secrets
                  key: DATABASE_URL_GOAL
