apiVersion: apps/v1
kind: Deployment
metadata:
  name: analytics-embedding-consumer
  namespace: finance-tracker
spec:
  replicas: 1
  selector:
    matchLabels: { app: analytics-embedding-consumer }
  template:
    metadata:
      labels: { app: analytics-embedding-consumer }
    spec:
      containers:
        - name: analytics-embedding-consumer
          image: finance-tracker/analytics-service:local
          imagePullPolicy: Never
          command: ["python", "-m", "app.workers.embedding_consumer"]
          envFrom:
            - configMapRef:
                name: finance-tracker-config
            - secretRef:
                name: finance-tracker-secrets
