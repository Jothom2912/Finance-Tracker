apiVersion: apps/v1
kind: Deployment
metadata:
  name: postgres-banking
  namespace: finance-tracker
spec:
  replicas: 1
  selector:
    matchLabels: { app: postgres-banking }
  template:
    metadata:
      labels: { app: postgres-banking }
    spec:
      containers:
        - name: postgres
          image: postgres:16
          env:
            - name: POSTGRES_USER
              value: banking_service
            - name: POSTGRES_PASSWORD
              value: banking_service_pass
            - name: POSTGRES_DB
              value: banking_db
          ports:
            - containerPort: 5432
          volumeMounts:
            - name: data
              mountPath: /var/lib/postgresql/data
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: postgres-banking-data
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-banking-data
  namespace: finance-tracker
spec:
  accessModes: ["ReadWriteOnce"]
  resources:
    requests:
      storage: 1Gi
---
apiVersion: v1
kind: Service
metadata:
  name: postgres-banking
  namespace: finance-tracker
spec:
  selector: { app: postgres-banking }
  ports:
    - port: 5432
      targetPort: 5432