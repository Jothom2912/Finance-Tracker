apiVersion: apps/v1
kind: Deployment
metadata:
  name: postgres-transactions
  namespace: finance-tracker
spec:
  replicas: 1
  selector:
    matchLabels: { app: postgres-transactions }
  template:
    metadata:
      labels: { app: postgres-transactions }
    spec:
      containers:
        - name: postgres
          image: postgres:16
          env:
            - name: POSTGRES_USER
              value: transaction_service
            - name: POSTGRES_PASSWORD
              value: transaction_service_pass
            - name: POSTGRES_DB
              value: transactions
          ports:
            - containerPort: 5432
          volumeMounts:
            - name: data
              mountPath: /var/lib/postgresql/data
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: postgres-transactions-data
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-transactions-data
  namespace: finance-tracker
spec:
  accessModes: ["ReadWriteOnce"]
  resources:
    requests:
      storage: 1Gi
---
apiVersion: v1
kind: Service
metadata:
  name: postgres-transactions
  namespace: finance-tracker
spec:
  selector: { app: postgres-transactions }
  ports:
    - port: 5432
      targetPort: 5432
