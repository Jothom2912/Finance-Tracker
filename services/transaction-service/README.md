# Transaction Service

Manages financial transactions, CSV imports, and planned transactions.

## Quick Start

```bash
pip install -r requirements.txt
uvicorn app.main:app --reload --port 8002
```

## Health Check

```
GET /health
```
