# FastAPI afhængigheder
fastapi
uvicorn[standard]
pydantic-settings
python-multipart
passlib[bcrypt]
python-jose[cryptography]

# Database afhængigheder
sqlalchemy

# MySQL Driver
pymysql 

requests

# Datahåndtering (til CSV import i transaction_service)
pandas
tabulate

# Elasticsearch (til søgning og analytics)
# Version 8.x required for compatibility with Elasticsearch server 7/8
# Version 9.x uses API version 9 which is not compatible with older servers
elasticsearch<9.0.0

# Neo4j (til graph database)
neo4j

# GraphQL (read gateway via Strawberry + FastAPI integration)
strawberry-graphql[fastapi]

# ANBEFALEDE TILLÆG
python-dotenv 
pydantic[email]  # Krævet for EmailStr validering i user.py

# TESTING AFHÆNGIGHEDER
pytest
pytest-cov
httpx  # Krævet for FastAPI TestClient
